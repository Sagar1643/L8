#include <stdio.h>

int main() {
    float number;

// added comment
 
    printf("Enter an integer: ");
    scanf("%f", &number);


    if (number % 2 == 0) {
        printf("%d is even.\n", number);
    } else {
        printf("%d is odd.\n", number);
    }

    return 0;
}


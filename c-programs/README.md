# C programming exercises

This folder contains standalone introductory C exercises that are unrelated to the website scaffold.

Each `.c` file can be compiled separately, for example:

```sh
gcc add.c -o add
./add
```

`even.txt` is retained as an older exercise draft for reference.
